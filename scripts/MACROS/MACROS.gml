#macro FRAME_RATE 120 //baked into game at compile time
#macro TILE_SIZE 16
#macro CARDINAL_DIR round(direction/90) //converts degrees into a 0-3 number. /45 would work for 8 directions, ie
#macro ROOM_START rGarden